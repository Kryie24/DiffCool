# DiffCool

## Start trainining: Python main.py 
### Folders trained_models will contain trained weights and synthesized results

## Folder data:
### -data
### --design % the generated random designs for training
### --external % the initial bare-chip heatmaps used for testing
### --heatmap % heatmaps at different layers of the random designs